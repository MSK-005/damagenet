model:
  name: "resnet34"
  in_channels: 6
  encoder_weights: "imagenet"
  attention_type: "scse"
classes:
  num_classes: 5
training:
  batch_size: 16
  accumulation_steps: 2
  learning_rate: 0.0001
  epochs: 50