model:
  name: "resnet34"
  in_channels: 6
  encoder_weights: "imagenet"

classes:
  num_classes: 3

stage1:
  in_channels: 3
  num_classes: 1
  epochs: 30
  batch_size: 16
  accumulation_steps: 2
  learning_rate: 0.0001
  num_workers: 4

stage2:
  in_channels: 6
  num_classes: 3
  epochs: 50
  batch_size: 16
  accumulation_steps: 2
  learning_rate: 0.00005
  num_workers: 4