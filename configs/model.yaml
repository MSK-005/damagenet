model:
  name: "resnet50"
  in_channels: 6
  encoder_weights: "imagenet"
  attention_type: "scse"
classes:
  num_classes: 4
training:
  batch_size: 16
  accumulation_steps: 8
  learning_rate: 0.0001
  epochs: 50
  num_workers: 2